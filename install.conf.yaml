- defaults:
    link:
      create: true
      relink: true

- shell:
  # - [git submodule update --init --recursive, Installing Submodules]
  # - [./scripts/install-llvm.py, Installing LLVM]
  - [echo 'test']

# - link:
    # ~/.dotvim: dotvim
    # ~/.config/nvim/init.lua:
    #   create: true

- clean: ['~']

- package:
  - git
  - linux: g++


# Prerequisites
# git, wget, curl
